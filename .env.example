# Copy to .env and adjust for your environment.
# For local single-machine dev you can leave this commented.
# For LAN (multiple computers) set this to the IP of the server machine.
# Example: REACT_APP_API_BASE=http://192.168.1.23:4000/api
# For hosted deployment set to your hosted API root (must end with /api)
# Example: REACT_APP_API_BASE=https://recipe-app-9ijm.onrender.com/api
REACT_APP_API_BASE=
